name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    strategy:
      matrix:
        platform: [linux/amd64, linux/arm64]
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Get version
      id: version
      run: |
        # Get version from tag, removing 'v' prefix if present
        VERSION=${GITHUB_REF#refs/tags/}
        VERSION=${VERSION#v}
        echo "version=${VERSION}" >> $GITHUB_OUTPUT
        echo "Version: ${VERSION}"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v6
      with:
        context: .
        file: ./Dockerfile
        platforms: ${{ matrix.platform }}
        push: true
        tags: |
          sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }}-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

  create-manifest:
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Get version
      id: version
      run: |
        # Get version from tag, removing 'v' prefix if present
        VERSION=${GITHUB_REF#refs/tags/}
        VERSION=${VERSION#v}
        echo "version=${VERSION}" >> $GITHUB_OUTPUT
        echo "Version: ${VERSION}"

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Create and push multi-arch manifest
      run: |
        docker buildx imagetools create -t sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }} \
          sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }}-amd64 \
          sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }}-arm64
        
        docker buildx imagetools create -t sharedvolume/nfs-server-controller:latest \
          sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }}-amd64 \
          sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }}-arm64

    - name: Generate manifests
      run: |
        # Ensure we have the correct architecture tools
        rm -rf bin/
        make controller-gen kustomize
        make manifests
        make build-installer IMG=sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }}

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          dist/install.yaml
        generate_release_notes: true
        make_latest: true
      env:
        GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}

    - name: Generate manifests
      run: |
        # Ensure we have the correct architecture tools
        rm -rf bin/
        make controller-gen kustomize
        make manifests
        make build-installer IMG=sharedvolume/nfs-server-controller:${{ steps.version.outputs.version }}

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        files: |
          dist/install.yaml
        generate_release_notes: true
        make_latest: true
      env:
        GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}
